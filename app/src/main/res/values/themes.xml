<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.AisleSubmissionJava" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.CustomSplashScreenTheme" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/white</item>
        <item name="windowSplashScreenAnimatedIcon">@mipmap/ic_launcher</item>
        <item name="windowSplashScreenAnimationDuration">300</item>
        <item name="postSplashScreenTheme">@style/Theme.AisleSubmissionJava</item>
    </style>
    
    <style name="Theme.AisleSubmissionJava.Button" parent="ShapeAppearanceOverlay.Material3.Button">
        <item name="android:textColor">@color/black</item>
        <item name="android:background">@drawable/curved_button</item>
        <item name="android:height">40dp</item>
        <item name="android:width">96dp</item>
        <item name="android:textSize">14sp</item>
        <item name="android:backgroundTint">@color/yellow</item>
    </style> 

    <style name="Theme.AisleSubmissionJava" parent="Base.Theme.AisleSubmissionJava" />
</resources>